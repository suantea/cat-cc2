# Cat CC2 使用手册

> 极简代理工具：**订阅一键连接**(Windows / macOS)。Rust + Tauri 2 外壳 + mihomo 内核。

## 一、软件简介

Cat CC2 是一款极简的代理客户端，核心使用方式只有一步：**粘贴订阅链接，点"一键连接"**。

- **轻量**：Tauri 2 外壳使用系统 WebView2 渲染界面，不捆绑 Chromium，占用低、启动快
- **自动选优**：连接后自动对所有节点测速，选用延迟最低的节点（`url-test` 代理组），节点故障自动切换
- **内核健壮**：mihomo 内核意外退出会自动重启（最多 3 次，1s/2s/4s 指数退避）
- **干净退出**：托盘"退出"自动停内核并还原系统代理；异常退出/强杀后，下次启动自动清理残留的死代理（自愈）

## 二、安装与启动

### Windows

1. **获取程序**：直接运行 `cat-cc2.exe`（release 版无终端窗口）；或按 README 的构建命令自行编译。
2. **放置内核**：将 `mihomo-core.exe`（Clash Meta 内核，windows-amd64-compatible）放到程序目录或项目根目录。
   > ⚠️ 内核文件名**必须是 `mihomo-core.exe`**。改成 `mihomo.exe` 会被部分杀软拦截，导致启动失败（EPERM）。
3. **启动**：双击 `cat-cc2.exe` → 主窗口打开 → 托盘出现图标。关闭窗口会最小化到托盘（不退出），退出请用托盘菜单。

### macOS

1. **获取程序**：下载 `Cat CC2_<版本>_<arch>.dmg`（arm64=Apple Silicon，x64=Intel），打开拖入"应用程序"。
2. **放置内核**：将 `mihomo-core`（Clash Meta 内核，darwin 对应架构版）放到程序目录或项目根目录。
   > 当前未签名（无 Apple Developer 证书），首次打开会触发 Gatekeeper 拦截：
   > - 方式一（推荐）：右键点击 App → "打开" → 确认"仍要打开"
   > - 方式二：终端执行 `xattr -dr com.apple.quarantine "/Applications/Cat CC2.app"` 解除隔离后双击打开
3. **启动**：打开 App → 点击"一键连接"时系统会弹授权框要求输入管理员密码（设置系统代理需要权限，输入一次后短期免重复）→ 托盘出现图标。

## 三、快速上手（3 步）

1. **粘贴订阅**：把订阅链接（URL）或节点内容粘贴到输入框。
   - 支持：URL 订阅、base64 节点列表、Clash YAML 订阅、6 种协议节点（vmess / ss / trojan / vless / socks / http）
   - 想先确认内容没问题，可以点"**解析预览**"查看节点数量和名字
2. **一键连接**：点绿色"一键连接"按钮。
   - 程序会自动：拉取订阅 → 解析节点 → 生成 mihomo 配置 → 校验 → 启动内核 → 设置系统代理
   - 连接成功后托盘变绿，状态区显示当前节点与延迟
3. **断开**：点红色"断开"按钮，或托盘菜单"退出"。
   - 断开 = 停内核 + 还原系统代理；退出还会关闭程序

## 四、订阅管理

- **支持格式**：

  | 格式 | 说明 |
  |------|------|
  | URL 订阅 | `https://...` 链接，程序自动拉取 |
  | base64 节点列表 | 标准 / URL-safe base64 均可 |
  | Clash YAML | 自动提取 `proxies:` 节点段 |
  | 单条节点 | vmess:// ss:// trojan:// vless:// socks:// http:// |

- **ss 插件**：支持 SIP002 plugin（obfs / v2ray-plugin），自动映射到内核配置
- **节点去重**：相同 类型/服务器/端口/凭证 的节点自动去重，不会重复测速
- **订阅源被墙**：拉取直连失败时，若内核已在运行会自动经本地代理重试；未连接时被墙的订阅源，把内容直接粘贴到输入框即可
- **自动记忆**：订阅链接保存在本地，下次打开自动预填
- **自动刷新**：连接期间每 6 小时自动重新拉取订阅并生效；拉取/解析失败时自动沿用旧节点，不影响使用（仅对 URL 订阅生效）；全节点失效时自动重新拉取

## 五、界面说明

| 区域 | 内容 |
|------|------|
| 连接区 | 订阅输入框、"一键连接"/"断开"、"解析预览"、CF 优选 IP 输入框（可选，多个换行/逗号分隔，最多 5 个，仅对 ws+tls 节点生效，保存后下次连接生效） |
| 运行状态区 | 状态（运行中/已停止）、当前节点、延迟、重启次数、最近错误；每 3 秒自动刷新 |
| 路由规则区 | 完整生效规则（只读，含 gfw 清单条数）+ 自定义规则（可编辑） |

**状态区常见显示**：
- 延迟 `—`：内核刚启动，测速还没出结果
- 最近错误：有内容时表示最近一次异常（如内核意外退出自动重启的记录）

## 六、路由规则详解

### 默认策略（无需配置）
1. **私有 IP 与国内常用域名** → 直连（内置默认规则）
2. **gfw 名单**（内置 4022 条被墙域名，可被 `config/routes_box/gfwlist.txt` 覆盖）→ **全部走代理**
3. **其余** → 默认直连（`MATCH,DIRECT`）

### 自定义规则（不想走代理的域名在这里加）
自定义规则**排在 gfw 名单之前**，优先级更高——即使某域名在 gfw 名单里，也能强制它直连。

| 效果 | 规则写法 |
|------|----------|
| 域名及其所有子域名直连 | `DOMAIN-SUFFIX,example.com,DIRECT` |
| 仅该域名直连 | `DOMAIN,example.com,DIRECT` |
| 域名含关键词就直连 | `DOMAIN-KEYWORD,corp,DIRECT` |
| 某 IP 网段直连 | `IP-CIDR,192.168.50.0/24,DIRECT` |
| 强制某域名走代理 | `DOMAIN-SUFFIX,example.com,auto` |

**示例**：让 `steamcommunity.com` 直连，在自定义规则里加一行：
```
DOMAIN-SUFFIX,steamcommunity.com,DIRECT
```
然后点"**保存并生效**"（连接中会自动重启内核立即生效）。

### 注意事项
- ⚠️ **一条规则只能写一个域名**（mihomo 的 `DOMAIN-SUFFIX` 不支持单条多域名）
- **不要添加 `MATCH` 行**（系统自动生成 `MATCH,DIRECT` 兜底）
- 规则写错会导致内核校验失败、拒绝启动（界面显示"配置校验失败"）
- 默认规则已含私有 IP（127/10/172.16/192.168）与常见国内站点直连，一般无需重复添加

## 七、异常退出与系统代理自愈（重要）

- **正常退出**：托盘"退出" = 停内核 + 还原系统代理，不留任何残留
- **异常退出/强杀/断电**：系统代理可能残留。**下次启动软件时自动检测并还原**（仅当代理指向本地端口且该端口无进程监听时才清理，不影响其他代理工具；macOS 上会扫描**全部**网络服务——若连接时走 Wi-Fi、退出前切到有线，旧服务上残留的死代理同样会被清理，多服务需清理时只弹一次授权框）
- **网页打不开怎么办**：
1. 打开 Cat CC2（会自动自愈残留代理）
2. Windows 手动改注册表：`HKCU\Software\Microsoft\Windows\CurrentVersion\Internet Settings` 的 `ProxyEnable` 改为 `0`；macOS 在"系统设置 → 网络 → 详细信息 → 代理"里关闭相关项
3. 确认状态区显示"运行中"、当前节点有值

## 八、常见问题（FAQ）

| 问题 | 解决办法 |
|------|----------|
| 网页打不开 / 无法上网 | 先看是否残留代理（见第七章自愈）；确认连接状态正常 |
| 连接成功但延迟一直 0 / 节点不可达 | 订阅节点质量问题：线路失效，换订阅或换节点测试 |
| 提示"未找到 mihomo-core.exe" | 内核文件缺失或不在程序目录/项目根；确认命名未改成 mihomo.exe |
| 提示"配置校验失败，已拒绝启动" | 自定义规则格式写错（多域名一行 / 手写了 MATCH 等），检查规则 |
| 提示"解析不到任何节点" | 订阅内容无效：检查订阅链接是否过期、格式是否支持 |
| 端口被占用 | mixed 7890 / external-controller 9090 被占时自动随机换端口，无需处理 |
| 杀软报毒/拦截 | 确保内核命名 mihomo-core.exe；给程序目录加白名单 |
| 如何确认当前走不走代理 | 状态区"当前节点"有值即走代理；"已停止"或节点为空则直连 |
| 规则改了没生效 | 需要点"保存并生效"；未连接时保存的规则下次连接生效 |

## 九、隐私与安全

- 系统代理只指向本地 `127.0.0.1`，流量不经过任何第三方中转
- 订阅链接含订阅 token，以明文保存在本地（WebView2 localStorage），请妥善保管，勿在公用电脑使用
- 本软件开源：https://github.com/suantea/cat-cc2（AtomGit 为镜像）

## 十、开发者信息

构建与测试命令（本机建议低并行，防线程资源耗尽）：

```bash
cargo check                                # 语法检查
cargo test --lib -j 2                       # 单元测试
CARGO_BUILD_JOBS=2 cargo test --test full_flow -j 2   # 全流程集成测试
CARGO_BUILD_JOBS=2 cargo build --release -j 2          # release 构建
```

更多技术细节见 `README.md` 与 `AGENTS.md`。

## 十一、三端配合：edgetunnel + fancyss + Cat CC2

服务端 **edgetunnel**（部署在 Cloudflare Workers/Pages 上的 VLESS 隧道）统一输出订阅；路由器 **fancyss** 与电脑端 **Cat CC2** 消费同一份订阅，节点变更只需改服务端一处，两端自动同步。

### edgetunnel 快速部署要点

- 在 Cloudflare **Workers/Pages** 上部署 edgetunnel，并**绑定 KV 命名空间**
- **必须绑定自定义域**（不要用 workers.dev，限速且易被风控）

常用环境变量：

| 环境变量 | 说明 |
|------|------|
| ADMIN | 面板密码 |
| KEY | 订阅路径密钥 |
| UUID | 固定节点 UUID |
| PROXYIP | 优选反代 IP |
| TCP_CONCURRENT_DIAL | 并发拨号，建议 2~3 |
| BEST_SUB | 开启优选订阅生成 |
| PRELOAD_RACE_DIAL | 预加载竞速拨号 |

### 三端配合用法

拿到订阅地址后填入 Cat CC2 订阅框；fancyss 插件「订阅」填同一地址。在家走路由器（fancyss 透明代理全家分流），出门/单独场景用 Cat CC2。两端均为 gfw 直连模式、规则思路一致，切换无感知。

### 优选 IP（提速）

三种方式：
1. **edgetunnel 开启 `BEST_SUB`** 生成优选订阅
2. **Cat CC2 自动优选 IP**：`opt_ip.txt` 为空时，连接会自动并发探测内置 CF Anycast 候选池（可用 `config/opt_ip_pool.txt` 覆盖追加），取延迟最低的前 5 个写入并生效；也可点界面"自动测速优选 IP"手动刷新
3. **Cat CC2 手动填 CF 优选 IP**：仅对 ws+tls 的 vless/vmess/trojan 节点生效，自动替换 server 并保留 Host 头

### 提速与稳定清单

- **服务端提速**：自定义域、PROXYIP、并发拨号、优选 IP
- **客户端稳定**：订阅自动刷新、全节点失效自动重拉、内核看门狗
