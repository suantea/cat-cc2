# Cat CC2 发行版发布流程

> 本文说明如何发布新版本（生成可下载的 exe 发行版）。**当前采用手动发布**（AtomGit 流水线暂不支持 Windows 构建，见"方式二"说明）。

## 方式一：手动发布（推荐，当前唯一可行）

### 步骤

1. 本机构建（**本机有线程资源限制，务必加 `CARGO_BUILD_JOBS=2` 和 `-j 2`**）：

   ```bash
   cd src-tauri && CARGO_BUILD_JOBS=2 cargo build --release -j 2
   ```

2. 产物：`src-tauri/target/release/cat-cc2.exe`
3. 打版本 tag 并推送：

   ```bash
   git tag v2.0.2        # 版本号按需修改
   git push origin v2.0.2
   ```

4. 打开 AtomGit → Releases → 对应 tag → 把 exe 拖入资产区（**手动上传**）
5. 补写发行说明（变更内容、平台、许可等）

## 方式二：CI 自动发布（备用，暂不可行）

仓库曾配置 `.atomgit/workflows/release.yml`（推送 v* tag → 构建 → 自动上传），但经官方文档核实：

- AtomGit 流水线（Actions）**暂无 Windows 运行器**（文档示例仅 `ubuntu-latest` 等 Linux 运行器）
- 本项目的 exe 必须 **Windows 构建**（系统代理 `proxy.rs` 为 Windows 专属：注册表 + wininet）
- 流水线使用内置 action（`uses: checkout` 等）与 `ATOMGIT_TOKEN`，且**不提供发行版资产上传**能力

→ **CI 自动构建发行版当前不可行**，配置已移除。若未来 AtomGit 增加 Windows 运行器，可恢复流水线配置（语法与 GitHub Actions 兼容）。

## 敏感信息说明

发行版 exe 只包含仓库代码与内置公开数据（gfw 名单、默认规则），**不含**：

- 订阅链接 / 节点凭证 —— 只在运行时 `config/temp/config.yaml` 与 WebView2 localStorage（均不入库、不进 exe）
- 用户自定义规则 —— 运行时 `config/rules.txt`（已 gitignore 本地改动）
- 发布前可扫描确认：

  ```bash
  grep -a "优选\|cheryholding" cat-cc2.exe   # 应输出 0 处
  ```

## 第三方合规

- 自身代码：**Apache-2.0**
- `mihomo-core.exe`：GPL-3.0 **独立组件**，发行版若捆绑须附许可声明与源码链接（已提供 `THIRD_PARTY_NOTICES.md`）
