# Cat CC2 发行版发布流程

> 本文说明如何发布新版本（生成可下载的 exe 发行版）。**推荐使用 CI 自动流程**，也可手动发布。

## 方式一：CI 自动发布（推荐）

仓库已配置流水线 `.atomgit/workflows/release.yml`：**推送版本 tag 即自动构建并上传发行版**。

> ⚠️ **首次使用前**：需在仓库的「流水线」页面**启用流水线功能**（设置 → 开启），启用后需做一次提交触发系统同步（本次 workflow 文件的提交即可）。AtomGit 只扫描 `.atomgit/workflows/` 目录（不是 GitHub 的 `.github/workflows/`）。

### 步骤

1. 确认代码已合并到 `main` 且本机构建通过：

   ```bash
   cd src-tauri && CARGO_BUILD_JOBS=2 cargo build --release -j 2
   ```

2. 打版本 tag 并推送：

   ```bash
   git tag v2.0.1            # 版本号按需修改
   git push origin v2.0.1
   ```

3. 流水线自动执行（约 10-20 分钟）：Windows 构建机 `cargo build --release` → 生成 `cat-cc2.exe` → 上传到该 tag 的发行版页面
4. 到 AtomGit 仓库 **Releases 页面**核对：发行版自动生成，exe 已附在资产区

### 触发方式

- 推送 `v*` 格式的 tag（如 `v1.0.0`、`v2.0.0`）
- 或仓库"流水线"页面手动运行（workflow_dispatch）

### 注意事项

- 构建平台固定为 **Windows**（项目的系统代理实现为 Windows 专属，macOS/Linux 不构建）
- 若 AtomGit 流水线没有 Windows 运行器（runner），CI 构建将无法进行 → 改用手动发布（方式二）
- 若上传步骤失败（第三方 action 兼容问题）：改用手动方式发布（见下），或查看流水线日志定位
- tag 描述若被内容审核拦截（网络相关词），发行说明请在网页上手动补充

## 方式二：手动发布（备选）

1. 本机构建（**本机有线程资源限制，务必加 `CARGO_BUILD_JOBS=2` 和 `-j 2`**）：

   ```bash
   cd src-tauri && CARGO_BUILD_JOBS=2 cargo build --release -j 2
   ```

2. 产物：`src-tauri/target/release/cat-cc2.exe`
3. 打 tag：`git tag v2.0.0 && git push origin v2.0.0`
4. 打开 AtomGit → Releases → 对应 tag → 把 exe 拖入资产区（手动上传）
5. 补写发行说明（变更内容等）

## 敏感信息说明

发行版 exe 只包含仓库代码与内置公开数据（gfw 名单、默认规则），**不含**：

- 订阅链接 / 节点凭证 —— 只在运行时 `config/temp/config.yaml` 与 WebView2 localStorage（均不入库、不进 exe）
- 用户自定义规则 —— 运行时 `config/rules.txt`（已 gitignore 本地改动）
- 发布前可扫描确认：

  ```bash
  grep -a "优选\|cheryholding" cat-cc2.exe   # 应输出 0 处
  ```

## 第三方合规

- 自身代码：**Apache-2.0**
- `mihomo-core.exe`：GPL-3.0 **独立组件**，发行版若捆绑须附许可声明与源码链接（已提供 `THIRD_PARTY_NOTICES.md`）
