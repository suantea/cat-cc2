# Cat CC2 发行版发布流程

> 本文说明如何发布新版本（生成可下载的发行版）。**主仓库为 GitHub**（AtomGit 为镜像）；
> CI 自动发布走 GitHub Actions（`.github/workflows/release.yml`）。

## 方式一：CI 自动发布（推荐）

打 `v*` tag 推送到 GitHub 即可，Actions 会自动构建并上传到 GitHub Releases：

```bash
git tag v2.1.1         # 版本号按需修改
git push origin main
git push origin v2.1.1
```

- **产物**（tag 对应的 Release 资产区自动出现）：
  - `CatCC2-<版本>-windows-x64.exe`
  - `CatCC2-<版本>-aarch64-apple-darwin`（macOS Apple Silicon）
  - `CatCC2-<版本>-x86_64-apple-darwin`（macOS Intel）
- 构建矩阵在 `.github/workflows/release.yml`（windows-latest + macos-latest，Rust 缓存）。
- macOS 的 `.app`/`.dmg` 打包需要 tauri-cli，仍走方式二本地执行；CI 产物为裸二进制
  （Tauri 前端资产 dist/ 已入库，直接运行可用）。

## 方式二：本地手动发布（正式打包 / 补充产物）

1. 本机构建（**本机有线程资源限制，务必加 `CARGO_BUILD_JOBS=2` 和 `-j 2`**）：

   ```bash
   cd src-tauri && CARGO_BUILD_JOBS=2 cargo build --release -j 2
   ```

2. 产物：Windows `src-tauri/target/release/cat-cc2.exe`；macOS 完整打包
   `cargo tauri build` 产出 `.app` / `.dmg`（需安装 tauri-cli）。
3. 打开 GitHub → Releases → 对应 tag（没有就新建 draft）→ 把产物拖入资产区
4. 补写发行说明（变更内容、平台、许可等）

> 历史说明：AtomGit 流水线曾作为发布通道，但其 Actions 暂无 Windows 运行器、
> 也无发行版资产上传能力，CI 自动发布不可行——配置已移除。GitHub 转为主仓库后
> 自动发布由 GitHub Actions 承担。

## 敏感信息说明

发行版产物只包含仓库代码与内置公开数据（gfw 名单、默认规则），**不含**：

- 订阅链接 / 节点凭证 —— 只在运行时 `config/temp/config.yaml` 与 WebView2/WKWebView localStorage（均不入库、不进产物）
- 用户自定义规则 —— 运行时 `config/rules.txt`（已 gitignore 本地改动）
- 发布前可扫描确认：

  ```bash
  grep -a "优选\|cheryholding" cat-cc2.exe   # 应输出 0 处
  ```

## 第三方合规

- 自身代码：**Apache-2.0**
- `mihomo-core.exe`：GPL-3.0 **独立组件**，发行版若捆绑须附许可声明与源码链接（已提供 `THIRD_PARTY_NOTICES.md`）

## 仓库同步（GitHub 主 + AtomGit 镜像）

主仓库为 GitHub（`origin`）。AtomGit 为镜像，需同步时推送对应 remote（本地如未配置，
先 `git remote add atomgit https://atomgit.com/asuan_suan/cat-cc2.git`）：

```bash
git push origin main
git push origin --tags     # 触发 GitHub Actions 自动发布
git push atomgit main
git push atomgit --tags
```
